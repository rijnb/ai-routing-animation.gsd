---
phase: 04-stats-and-marker-interaction
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - src/__tests__/stats.test.ts
  - src/__tests__/markerDrag.test.ts
autonomous: true
requirements:
  - STAT-01
  - STAT-02
  - STAT-03
  - MAP-04

must_haves:
  truths:
    - "stats.test.ts exists with RED failing tests for nodesExplored reset, distance km formatting, and estimateTravelTime per mode"
    - "markerDrag.test.ts exists with RED failing tests for handleMarkerDrag calling triggerRoute with correct snap result"
    - "npx vitest run fails with test failures (not import/syntax errors)"
  artifacts:
    - path: "src/__tests__/stats.test.ts"
      provides: "Wave 0 test stubs for STAT-01, STAT-02, STAT-03"
      exports: ["nodesExplored tests", "distance formatting tests", "travelTime tests"]
    - path: "src/__tests__/markerDrag.test.ts"
      provides: "Wave 0 test stub for MAP-04 drag callback pipeline"
      exports: ["handleMarkerDrag tests"]
  key_links:
    - from: "src/__tests__/stats.test.ts"
      to: "src/lib/routeStats.ts"
      via: "import estimateTravelTime, formatDistance"
      pattern: "import.*routeStats"
    - from: "src/__tests__/markerDrag.test.ts"
      to: "src/hooks/useRouter.ts"
      via: "import handleMarkerDrag"
      pattern: "import.*useRouter"
---

<objective>
Create RED Wave 0 test stubs for all Phase 4 pure-function behaviors before any implementation exists.

Purpose: Enforce TDD contract — tests define the API, implementation follows. Tests must fail with "module not found" or assertion failures, never syntax errors.
Output: Two test files with explicit behavior assertions ready to go GREEN in Wave 1.
</objective>

<execution_context>
@/Users/ribu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ribu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-stats-and-marker-interaction/04-RESEARCH.md

<interfaces>
<!-- Key types the tests import against. Extracted from codebase. -->

From src/lib/router.ts:
```typescript
export type RoutingMode = 'car' | 'bicycle' | 'pedestrian'

export interface RouteResult {
  path: [number, number][]
  searchHistory: string[]
  distance: number  // total meters
  found: boolean
}
```

From src/lib/animationUtils.ts:
```typescript
export function filterHistory(searchHistory: string[]): string[]
// returns history filtered to unique entries (used to get total node count)
```

From src/hooks/useRouter.ts — handleMarkerDrag will be added here in Plan 03:
```typescript
// NEW function to be created in Plan 03:
// handleMarkerDrag(which: 'source' | 'destination', lngLat: [number, number]) => void
```

<!-- New module to create in Plan 02: -->
// src/lib/routeStats.ts (Wave 1 will create this):
// export function estimateTravelTime(distanceMeters: number, mode: RoutingMode): number
// export function formatDistance(meters: number): string   // e.g. "3.14 km"
// export function formatTime(seconds: number): string      // e.g. "45 min" or "2h 15m"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create stats.test.ts RED stubs</name>
  <files>src/__tests__/stats.test.ts</files>
  <behavior>
    - Test STAT-02: formatDistance(3141) returns "3.14 km" (divide by 1000, 2 decimal places)
    - Test STAT-02: formatDistance(500) returns "0.50 km"
    - Test STAT-03: estimateTravelTime(50000, 'car') returns 3600 (50km at 50km/h = 1 hour)
    - Test STAT-03: estimateTravelTime(15000, 'bicycle') returns 3600 (15km at 15km/h = 1 hour)
    - Test STAT-03: estimateTravelTime(5000, 'pedestrian') returns 3600 (5km at 5km/h = 1 hour)
    - Test STAT-03: estimateTravelTime(1000, 'car') returns 72 (1km at 50km/h = 72 seconds)
    - Test STAT-01: formatTime(3660) returns "1h 1m" (>= 3600s uses hours format)
    - Test STAT-01: formatTime(2700) returns "45 min" (< 3600s uses minutes format)
    - Test STAT-01: formatTime(59) returns "1 min" (rounds, minimum 1 min)
  </behavior>
  <action>
    Import `estimateTravelTime`, `formatDistance`, `formatTime` from `'../lib/routeStats'` — this module does NOT exist yet. The import itself will cause the test to fail RED until Plan 02 creates it. Write each test with specific input/output pairs as listed in behavior. Use vitest `describe` and `it` blocks matching the test filter strings referenced in VALIDATION.md: group under `describe('distance', ...)`, `describe('travelTime', ...)`, `describe('nodesExplored', ...)`.

    For the nodesExplored group (STAT-01 counter behavior): Since `nodesExplored` is internal to `useAnimation` rAF loop, the unit test here covers `formatTime` formatting edge cases (which is the pure-function aspect of STAT-01). Add a test confirming `formatTime(0)` returns `"0 min"`.

    Do NOT import from React or any DOM library — these are pure function tests.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/stats.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>stats.test.ts exists; `npx vitest run src/__tests__/stats.test.ts` fails with "Cannot find module '../lib/routeStats'" or assertion failures — not a syntax error. At least 9 test cases defined.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create markerDrag.test.ts RED stubs</name>
  <files>src/__tests__/markerDrag.test.ts</files>
  <behavior>
    - Test MAP-04: When handleMarkerDrag('source', [4.9, 52.3]) is called and a valid snap exists, triggerRoute is called with the new snap as source and existing destSnap
    - Test MAP-04: When handleMarkerDrag('destination', [4.9, 52.3]) is called and a valid snap exists, triggerRoute is called with existing sourceSnap and new snap as destination
    - Test MAP-04: When snapToNearestSegment returns null (no road within 200m), triggerRoute is NOT called
    - Test MAP-04: When only source is set (no destSnap) and source marker is dragged, triggerRoute is NOT called (need both endpoints)
  </behavior>
  <action>
    The `handleMarkerDrag` function will live in `useRouter.ts` but is not exported yet. For testability, structure the test to import a standalone `createHandleMarkerDrag` factory function OR mock useRouter internals.

    Recommended approach (simpler for RED stub): Import `handleMarkerDrag` as a named export from `'../hooks/useRouter'` — this export does not exist yet, causing RED. Write tests using vi.fn() mocks for `triggerRoute`, `cancelAnimation`, `snapToNearestSegment`, and a mock `graph` object.

    Use `vi.mock('../lib/segmentSnap', ...)` to control snapToNearestSegment return values.

    The tests define the contract: `handleMarkerDrag` must accept `(which: 'source' | 'destination', lngLat: [number, number])` and must call or skip triggerRoute based on the snap result and existing state.

    Since handleMarkerDrag in useRouter is a hook-internal callback, the cleanest testable approach is to export a pure `buildHandleMarkerDrag` factory. Tests call that factory directly. Note this in a comment so Plan 03 knows to export this factory function.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/markerDrag.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>markerDrag.test.ts exists; `npx vitest run src/__tests__/markerDrag.test.ts` fails RED (import error or assertion failure). At least 4 test cases defined.</done>
</task>

</tasks>

<verification>
Full suite: `npx vitest run` — existing tests stay GREEN, two new test files are RED (failing for expected reasons: missing module or missing export).

Confirm RED status: both new files must show as FAIL in the test run output.
</verification>

<success_criteria>
- src/__tests__/stats.test.ts exists with 9+ test cases, all RED due to missing routeStats module
- src/__tests__/markerDrag.test.ts exists with 4+ test cases, all RED due to missing handleMarkerDrag export
- All previously passing tests remain GREEN
- No syntax errors in either test file
</success_criteria>

<output>
After completion, create `.planning/phases/04-stats-and-marker-interaction/04-01-SUMMARY.md`
</output>
