---
phase: 03-search-animation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/__tests__/animation.test.ts
  - src/lib/animationUtils.ts
autonomous: true
requirements:
  - ANIM-01
  - ANIM-02
  - ANIM-03

must_haves:
  truths:
    - "Pure animation logic is unit-tested and GREEN before any rAF/MapLibre work begins"
    - "Virtual node IDs are filtered from searchHistory before coordinate lookup"
    - "Path slice fraction is correctly proportional to cursor/total"
    - "nodesPerFrame calculation is correct at both ends of the speed range"
  artifacts:
    - path: "src/lib/animationUtils.ts"
      provides: "Pure functions: filterHistory, slicePath, computeNodesPerFrame"
      exports: ["filterHistory", "slicePath", "computeNodesPerFrame"]
    - path: "src/__tests__/animation.test.ts"
      provides: "Unit tests covering ANIM-01, ANIM-02, ANIM-03 pure logic"
      contains: "filterHistory, slicePath, computeNodesPerFrame"
  key_links:
    - from: "src/__tests__/animation.test.ts"
      to: "src/lib/animationUtils.ts"
      via: "direct import"
      pattern: "from.*animationUtils"
---

<objective>
Extract and TDD the pure animation logic functions before implementing the stateful rAF hook.

Purpose: The rAF loop + MapLibre integration cannot be unit-tested (jsdom has no real rAF or WebGL). But the three core computations — filtering virtual nodes, slicing the path proportionally, and deriving nodesPerFrame — can be extracted as pure functions and tested directly. Getting these GREEN first means the hook implementation in Plan 02 has solid, verified building blocks.

Output: `src/lib/animationUtils.ts` (pure functions) and `src/__tests__/animation.test.ts` (GREEN).
</objective>

<execution_context>
@/Users/ribu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ribu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-search-animation/03-CONTEXT.md
@.planning/phases/03-search-animation/03-RESEARCH.md
</context>

<feature>
  <name>Animation pure-logic utilities</name>
  <files>src/lib/animationUtils.ts, src/__tests__/animation.test.ts</files>
  <behavior>
    filterHistory(searchHistory: string[]): string[]
    - Removes '__vs__' and '__ve__' entries from the array
    - ['__vs__', 'A', 'B', '__ve__'] → ['A', 'B']
    - ['A', 'B', 'C'] → ['A', 'B', 'C'] (no virtual nodes, unchanged)
    - [] → []

    slicePath(path: [number,number][], cursor: number, total: number): [number,number][]
    - Returns path.slice(0, ceil(path.length * (cursor / total)))
    - cursor=0, total=10 → [] (fraction 0)
    - cursor=5, total=10 → first 50% of path coordinates
    - cursor=10, total=10 → full path (fraction 1.0)
    - cursor >= total always returns full path
    - Empty path returns []

    computeNodesPerFrame(multiplier: number): number
    - Returns max(1, round(7 * multiplier))
    - multiplier=0.5 → max(1, round(3.5)) = 4
    - multiplier=1.0 → max(1, round(7)) = 7
    - multiplier=5.0 → max(1, round(35)) = 35
    - multiplier=0.1 → max(1, ...) = 1 (floor at 1)
  </behavior>
  <implementation>
    Create src/lib/animationUtils.ts with three exported pure functions.
    No imports needed — pure TypeScript, no dependencies.
    Run RED (tests fail with missing module), then implement GREEN.
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: Write RED tests for animation utilities</name>
  <files>src/__tests__/animation.test.ts</files>
  <action>
    Create `src/__tests__/animation.test.ts` importing `filterHistory`, `slicePath`, `computeNodesPerFrame` from `../../src/lib/animationUtils`.

    Write describe blocks for each function covering all behavior cases listed in the feature block above.

    Key test cases to include:
    - filterHistory: removes __vs__ and __ve__; leaves clean arrays untouched; handles empty array
    - slicePath: cursor=0 returns empty; cursor=total returns full path; cursor=5/total=10 returns ceil(length*0.5) coords; cursor>total clamps to full path; empty path returns []
    - computeNodesPerFrame: 0.5x→4, 1.0x→7, 5.0x→35, very low multiplier→min 1

    Run test — MUST fail with "Cannot find module" or similar. Confirm RED before proceeding.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/animation.test.ts 2>&1 | grep -E "FAIL|Cannot find module|Error"</automated>
  </verify>
  <done>Test file exists and fails with module-not-found (RED confirmed)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement animationUtils.ts (GREEN)</name>
  <files>src/lib/animationUtils.ts</files>
  <behavior>
    - filterHistory(['__vs__', 'A', 'B', '__ve__']) === ['A', 'B']
    - slicePath(coords, 5, 10) returns first ceil(len*0.5) entries
    - slicePath(coords, 10, 10) returns full coords
    - computeNodesPerFrame(0.5) === 4
    - computeNodesPerFrame(5.0) === 35
    - computeNodesPerFrame(0.1) === 1
  </behavior>
  <action>
    Create `src/lib/animationUtils.ts` with three exported pure functions:

    ```typescript
    export function filterHistory(searchHistory: string[]): string[] {
      return searchHistory.filter(id => id !== '__vs__' && id !== '__ve__')
    }

    export function slicePath(
      path: [number, number][],
      cursor: number,
      total: number,
    ): [number, number][] {
      if (total === 0 || path.length === 0) return []
      const fraction = Math.min(cursor / total, 1)
      return path.slice(0, Math.ceil(path.length * fraction))
    }

    export function computeNodesPerFrame(multiplier: number): number {
      return Math.max(1, Math.round(7 * multiplier))
    }
    ```

    Run tests — MUST be GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/animation.test.ts</automated>
  </verify>
  <done>All animation utility tests pass GREEN. `npx vitest run` (full suite) is also GREEN.</done>
</task>

</tasks>

<verification>
Full test suite passes: `npx vitest run`
animationUtils.ts exports three named functions with no side effects.
</verification>

<success_criteria>
- src/lib/animationUtils.ts exists with filterHistory, slicePath, computeNodesPerFrame
- src/__tests__/animation.test.ts is GREEN (all test cases pass)
- npx vitest run passes with no new failures
</success_criteria>

<output>
After completion, create `.planning/phases/03-search-animation/03-01-SUMMARY.md`
</output>
