---
phase: 01-data-pipeline-and-map-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/__tests__/osmParser.test.ts
  - src/__tests__/graphBuilder.test.ts
  - src/__tests__/osmPipeline.test.ts
  - src/__tests__/dropZone.test.ts
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-04

must_haves:
  truths:
    - "Vite dev server starts without errors"
    - "TypeScript compiles with no errors"
    - "Vitest can be invoked and discovers test files"
    - "All Wave 0 test stubs exist and fail (RED) — implementation not yet written"
  artifacts:
    - path: "vite.config.ts"
      provides: "Build config with Vitest jsdom environment"
      contains: "test: { environment: 'jsdom', globals: true }"
    - path: "src/__tests__/osmParser.test.ts"
      provides: "Failing test stubs for PIPE-02 parse correctness"
    - path: "src/__tests__/graphBuilder.test.ts"
      provides: "Failing test stubs for PIPE-04 GeoJSON output"
    - path: "src/__tests__/osmPipeline.test.ts"
      provides: "Failing test stubs for PIPE-02 gunzip+parse integration"
    - path: "src/__tests__/dropZone.test.ts"
      provides: "Failing test stubs for PIPE-01 file handler"
  key_links:
    - from: "vite.config.ts"
      to: "src/__tests__/*.test.ts"
      via: "vitest test runner configured with jsdom"
      pattern: "environment.*jsdom"
---

<objective>
Bootstrap the Vite + React + TypeScript project with all dependencies installed and Vitest test infrastructure configured. Create Wave 0 failing test stubs for every requirement so subsequent plans can implement against known contracts.

Purpose: Nothing else can run without the project scaffold. Test stubs define the contracts (RED state) that implementation plans must satisfy (GREEN).
Output: Runnable project, configured test runner, four failing test files covering PIPE-01, PIPE-02, PIPE-04.
</objective>

<execution_context>
@/Users/ribu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ribu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-pipeline-and-map-foundation/01-CONTEXT.md
@.planning/phases/01-data-pipeline-and-map-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React + TypeScript project with all dependencies</name>
  <files>package.json, vite.config.ts, tsconfig.json, index.html, src/main.tsx, src/App.tsx</files>
  <action>
    Run the following sequence from the project root (/Users/ribu/source/rijnb/ai-routing-animation.gsd):

    1. Scaffold the project:
       ```
       npm create vite@latest . -- --template react-ts --force
       ```
       (Use --force to scaffold into existing directory with only public/ and .planning/)

    2. Install runtime dependencies:
       ```
       npm install maplibre-gl@^5.20.0 fflate@^0.8.2
       ```

    3. Install dev dependencies:
       ```
       npm install -D vitest@^2.x jsdom @testing-library/react@^16.x @testing-library/jest-dom @testing-library/user-event
       ```

    4. Configure vite.config.ts — add the test section to the existing Vite config:
       ```typescript
       import { defineConfig } from 'vite'
       import react from '@vitejs/plugin-react'

       export default defineConfig({
         plugins: [react()],
         test: {
           environment: 'jsdom',
           globals: true,
           setupFiles: ['./src/__tests__/setup.ts'],
         },
       })
       ```

    5. Create src/__tests__/setup.ts:
       ```typescript
       import '@testing-library/jest-dom'
       ```

    6. Replace the default src/App.tsx with a minimal stub:
       ```typescript
       export default function App() {
         return <div id="app-root">OSM Routing Animator</div>
       }
       ```

    7. Update tsconfig.json to include test files in compilation:
       Ensure "include" covers "src/**/*" (default Vite scaffold already does this).

    Do NOT add any application logic to App.tsx here — that is Plan 03's job.
    The public/maps/ directory already contains leiden.osm.gz and amsterdam.osm.gz — do not modify these.
  </action>
  <verify>
    <automated>cd /Users/ribu/source/rijnb/ai-routing-animation.gsd && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>npm run build completes without errors; package.json lists maplibre-gl, fflate as dependencies and vitest, jsdom as devDependencies</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Wave 0 failing test stubs (RED state)</name>
  <files>
    src/__tests__/osmParser.test.ts,
    src/__tests__/graphBuilder.test.ts,
    src/__tests__/osmPipeline.test.ts,
    src/__tests__/dropZone.test.ts
  </files>
  <behavior>
    osmParser.test.ts:
    - Test: parseOsmXml with minimal OSM XML fixture returns nodes Map with correct lon/lat (GeoJSON order: longitude first)
    - Test: parseOsmXml filters only highway ways from ROAD_TYPES set
    - Test: parseOsmXml way with fewer than 2 nodes is excluded
    - Test: longitude stored as index 0 (not latitude) — guard against lat/lon swap

    graphBuilder.test.ts:
    - Test: buildRoadGeoJson returns GeoJSON FeatureCollection
    - Test: each Feature has geometry.type === 'LineString'
    - Test: each Feature has properties.highway set to the OSM highway value
    - Test: empty ways input produces FeatureCollection with zero features

    osmPipeline.test.ts:
    - Test: gunzipSync on a gzip-encoded Uint8Array returns decompressed bytes (use fflate gzipSync to create fixture)
    - Test: full pipeline (gunzip → parseOsmXml) on a small synthetic .osm.gz fixture produces at least one highway way

    dropZone.test.ts:
    - Test: handleFile function exists and accepts a File object
    - Test: handleFile calls postMessage on the worker with an ArrayBuffer (mock the worker)
  </behavior>
  <action>
    Create four test files with the stubs described in the behavior section. Each test file imports the module under test using the path that Plan 02 will create:
    - osmParser.test.ts imports from '../lib/osmParser'
    - graphBuilder.test.ts imports from '../lib/graphBuilder'
    - osmPipeline.test.ts imports fflate directly (already installed) and '../lib/osmParser'
    - dropZone.test.ts imports from '../lib/osmLoader' (the handleFile function)

    Since Plan 02 has not yet created these modules, ALL tests must fail with "Cannot find module" or similar import errors in RED state. Do NOT create the implementation files.

    For the OSM XML fixture in osmParser.test.ts, use a minimal inline XML string:
    ```
    const FIXTURE = `<?xml version="1.0"?>
    <osm>
      <node id="1" lat="52.3" lon="4.9"/>
      <node id="2" lat="52.31" lon="4.91"/>
      <way id="10">
        <nd ref="1"/><nd ref="2"/>
        <tag k="highway" v="primary"/>
      </way>
    </osm>`
    ```

    Note the coordinate order assertion: node "1" must be stored as [4.9, 52.3] (lon, lat — GeoJSON order), NOT [52.3, 4.9]. This catches the #1 pitfall.

    For osmPipeline.test.ts, use fflate's gzipSync to create a synthetic .osm.gz fixture in-memory (do not read from disk in tests — keeps tests fast and hermetic).
  </action>
  <verify>
    <automated>cd /Users/ribu/source/rijnb/ai-routing-animation.gsd && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Vitest runs and discovers all 4 test files; tests fail due to missing modules (not syntax errors); test count matches behavior block</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0
- `npx vitest run --reporter=verbose` finds 4 test files and reports failures (not "no tests found")
- package.json contains: maplibre-gl, fflate in dependencies; vitest, jsdom, @testing-library/react in devDependencies
- vite.config.ts contains `environment: 'jsdom'` and `globals: true`
- src/__tests__/setup.ts imports @testing-library/jest-dom
</verification>

<success_criteria>
Project builds. Vitest discovers all 4 test stub files. All tests fail due to missing implementation modules (RED state confirmed). No syntax errors in test files.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-pipeline-and-map-foundation/01-01-SUMMARY.md`
</output>
